

- 05:30 ㅊㅊ

- 05:40 솔지: 구글 클라우드 스터디잼

- 08:00 박영세: 학습 계획, 시험, 달리기

- 09:30 허준 b1-1 eval


- b2-1 파이썬 가계부 콘솔
    - ultra

- 14:30~16:00 2-2 동재 종석 

- study
    - C
    - OS



---


## ultra 모르는 거

- category 삭제 시 사용 중인 카테고리 처리가 되는가?
- 2개 이상 클래스의 책임 경계를 어떻게 정했는지
- 2개 이상 클래스의 책임 경계를 어떻게 정했는지
- 제너레이터를 왜 쓰고, 어떻게 구현했는지
- 10만 건으로 늘어나면 어디가 병목이고 어떻게 개선하는지
- 타입 힌트의 이점을 실제 코드 예로 설명할 수 있는지
- 모듈 구조 models거래 내역서"라는 서류 양식(폼)의 규격을 정의, service "이 요청을 어떻게 처리할지" 두뇌가 판단 - 카테고리 삭제해도 되는지? 예산 초과했는지? 등


- 2.2.3 네임스페이스?
- 3.1 dataclass
- 3.3.4 @classmethod
- 3.6.2 Generator[Transaction, None, None]:  # Transaction을 하나씩 yield하는 제너레이터 반환
- 

✅ models.py는 데이터의 "양식(Form)"을 정의하는 곳이다
✅ @dataclass는 __init__, __repr__ 등을 자동 생성해주는 편리한 도구이다
✅ Transaction은 7개 필드(id, type, date, amount, category, memo, tags)를 가진다
✅ to_dict()는 객체→딕셔너리 변환(직렬화), from_dict()는 딕셔너리→객체 변환(역직렬화)
✅ field(default_factory=list)는 "가변 기본값의 함정"을 방지하는 안전장치이다
✅ 타입 힌트는 문서 역할 + IDE 자동완성 + 정적 분석 세 가지 이점을 제공한다
✅ Optional[str]은 "str 또는 None", List[str]은 "문자열 리스트"를 의미한다
✅ 4개 모델(Transaction, Category, Budget, RecurringRule) 모두 동일한 to_dict/from_dict 패턴을 따른다

---

## 알아둘 점

- 3.3.2 strptime이 하는 일: "2026-02-30"처럼 실제로 존재하지 않는 날짜를 넣으면 ValueError가 발생합니다. 단순한 정규식보다 훨씬 정확합니다.
- 3.3.2 왜 tags: List[str] = []가 아닌가?/ 파이썬에는 **"가변 기본값의 함정(Mutable Default Argument Trap)"**이라는 유명한 버그가 있습니다
- 



---

## task

- 문의: 특강