
feedback
```text
1. 타인이 읽기 좋은 코드와, 내가 읽기 좋은 코드는 다른코드라고 생각합니다.
 - 현재 문제점이라고 생각되는 부분들은 인덴트
 - 주석은 닥스트링을 이용하는게 좋습니다.
 - 코드에서, 설계에 대한 관점을 이야기 드렸습니다. 공통이 되는 부분들 혹은 기능분할을 할 수 있는 부분들을 잘 쪼갰을때 유지보수 및 보기 좋은 코드가 작성된다고 생각합니다.
 - 상수로 작성된 코드는 하나도 읽히지 않습니다. 상수는 따로 빼서 읽을 수 있는 스트링 객체로 만들어두는게 좋은것 같습니다.
 - 코드를 읽을 수 있는 명세로 만드는건 상당히 좋은 가독성을 만듭니다.

 - 삼항연산자 최소한으로 쓴다.
 - 리스트 컴프리헨션 거의 사용하지 않는다.
 - 분기따는 법을 연습한다. ex) if, elif, else 

2. 예쁜 함수 설계하기
 - 예외를 무조건 최상단에 넣어라!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
  1. 데이터 정제
  2. 데이터 유효성 검사
  3. 로직 구현

3. 아직 통신에 대한 학습 과정을 진행하지 않으셨으리라 생각합니다. status코드에 대해 설명을 드렸고, 이걸 가지고 어떻게 예외처리를 해야 하는지? 에러코드 반한되었을땐 메시지를 어떻게 열어보는지?
 또한 이 객체가 어떠한 형태로 되어 있는지 알려드렸습니다!

4. 바이브코딩을 통한 학습을 어떻게 해야 하는지에 대해서 질문하는 방식에 대해서 간략하게 설명을 드렸습니다.

+ @main.py gemma 인터넷 상태코드 예외처리 할 필요 없고, 그냥 정보 따오는 것 영어메시지와 이름 등 담긴 변수 세 가지 보여주면 됨.

+ 클래스를 사용하세요. 텍스트 클래스로 상수 스트링을 대체할 수 있습니다.

+ 주석은 docstring을 사용하세요.
```

---

# 🤖 에이전트 리팩토링 지시 프롬프트 (Refactoring Instruction Prompt)

이 프롬프트는 위의 피드백 내용을 기반으로 에이전트가 `main.py` 리팩토링 및 환경 자동화를 완벽히 수행할 수 있도록 체계적으로 정리된 명세입니다.

---

## 🎯 리팩토링 목표
코드의 가독성, 예외 처리 구조, 모듈화 수준을 한 단계 끌어올리고, 환경 설정을 자동화하여 타인이 프로젝트를 즉시 실행하고 이해할 수 있는 생산성 높은 구조로 개편합니다.

---

## ⚙️ 과제 0. 가상환경 및 의존성 설정 자동화

### 1. `setup.sh` 스크립트 작성
- **목표**: 누구나 스크립트 실행 한 번으로 가상환경 구축을 완료할 수 있도록 합니다.
- **요구사항**:
  - `requirements.txt` 또는 `req.txt`가 있는지 확인합니다.
  - `.venv` 가상환경이 없다면 생성(`python3 -m venv .venv`)합니다.
  - 가상환경을 활성화하고 최신 `pip`를 설치한 뒤, `req.txt`에 기재된 의존성 패키지(`python-dotenv`, `PyYAML`)를 자동 설치하는 스크립트를 작성하십시오.
  - 실행 권한을 부여하는 조치를 취해두거나 실행 방법을 가이드합니다.

### 2. `main.py` 내의 `ImportError` 가드 코드 제거
- **목표**: 가상환경을 통한 의존성 주입이 보장되므로, 런타임 중에 라이브러리 부재를 동적으로 대응하던 지저분한 폴백 코드를 제거하여 가독성을 높입니다.
- **요구사항**:
  - `python-dotenv`와 `PyYAML`은 필수 환경 사양으로 강제합니다.
  - `main.py` 상단의 아래 `try-except ImportError` 블록을 완전히 삭제하고 깔끔하게 표준 `import`만 남기십시오.
    - [main.py:L15-37](file:///Users/f22losophysics1091/Desktop/62api/main.py#L15-37) (`dotenv` 관련 수동 파싱 폴백 로직 제거)
    - [main.py:L42-47](file:///Users/f22losophysics1091/Desktop/62api/main.py#L42-47) (`yaml` 관련 `ImportError` 예외 방지 제거 및 `import yaml` 단일화)

---

## ✍️ 과제 1. 코드 가독성 및 안티패턴 개선

### 1. 인덴트 및 중첩 분기문 개선
- 과도한 들여쓰기(Indent)가 발생하는 구조를 정리하여 플랫(Flat)한 가독성을 확보합니다.

### 2. 문법 및 연산 단순화 (삼항 연산자 및 컴프리헨션 지양)
- **삼항 연산자 최소화**: 한 줄로 작성된 삼항 연산자를 직관적인 `if-else` 분기문으로 명확히 표현합니다.
  - 대상 예시: `masked_val = val[:2] + '*' * (len(val) - 2) if len(val) > 4 else '****'` ([main.py:L151](file:///Users/f22losophysics1091/Desktop/62api/main.py#L151))
- **리스트 컴프리헨션 지양**: 조건 처리가 섞여 가독성이 떨어지는 컴프리헨션이나 복잡한 제너레이터 표현식은 `for` 루프와 `if` 문으로 풀어서 해석하기 쉽게 만듭니다.
  - 대상 예시 1: `not api_key.isalnum() and not all(c in api_key for c in '-_')` ([main.py:L204](file:///Users/f22losophysics1091/Desktop/62api/main.py#L204))
  - 대상 예시 2: `actual_parts = [p['text'] for p in parts if not p.get('thought')]` ([main.py:L273](file:///Users/f22losophysics1091/Desktop/62api/main.py#L273))

### 3. 클래스를 사용한 상수 스트링(텍스트) 대체
- **목표**: 프롬프트 템플릿 등 하드코딩된 대형 문자열 상수를 텍스트 관리용 클래스로 추상화하여 분리합니다.
- **요구사항**:
  - `generate_commit_message` ([main.py:L333-353](file:///Users/f22losophysics1091/Desktop/62api/main.py#L333-353)) 및 `generate_pr_draft` ([main.py:L385-402](file:///Users/f22losophysics1091/Desktop/62api/main.py#L385-402)) 내부의 프롬프트 문자열 상수를 하드코딩하지 마십시오.
  - 템플릿 텍스트 상수를 관리하는 **전용 텍스트 클래스(예: `PromptTemplates` 클래스)**를 정의하고, 클래스 속성(Class Attribute) 형태로 관리하여 코드 가독성과 명세성을 극대화하십시오.
 
### 4. 주석은 Docstring을 사용
- **목표**: 코드 이해를 돕기 위해 사용되는 수많은 인라인 주석(`# ...`)을 걷어내거나 최소화하고, 파이썬 표준 **Docstring (`""" ... """`)**을 함수 및 클래스 선언 바로 아래에 추가하여 코드 스펙을 명세 형태로 기록하십시오.

---

## 🛡️ 과제 2. Guard Clauses (Early Return) 패턴 적용

- **목표**: 모든 함수는 예외 처리를 최상단에서 먼저 걸러내어, 유효한 케이스만 아래 메인 로직으로 흘러가게 설계합니다.
- **함수 내부 흐름 표준화**:
  1. **데이터 정제 (Cleanse)**: 인자값 공백 제거, 소문자화 등 데이터 정리.
  2. **데이터 유효성 검사 (Validate / Early Return)**: API 키 유재 여부, 파라미터 값 범위 초과 등의 예외 상황을 먼저 검출하여 에러 출력 후 조기 리턴(`return` 또는 `sys.exit`).
  3. **메인 로직 실행 (Execute)**: 검증을 통과한 데이터만 가지고 비즈니스 로직을 수행.

- **적용 대상**:
  - `call_gemini_api` ([main.py:L190](file:///Users/f22losophysics1091/Desktop/62api/main.py#L190)): API 키 검증 후 바로 API 요청을 처리하도록 흐름을 리팩토링하고, 중첩된 `try-except` 범위를 좁힙니다.
  - `apply_safe_mode` ([main.py:L94](file:///Users/f22losophysics1091/Desktop/62api/main.py#L94)): 인자 정제와 제한 검사를 맨 처음에 처리합니다.
  - `main` ([main.py:L428](file:///Users/f22losophysics1091/Desktop/62api/main.py#L428)): CLI 아규먼트 파싱 결과를 초기에 즉각 검증하여 에러 대응 로직을 함수 최상단에 배치합니다.

---

## 🌐 과제 3. 통신 예외처리 및 응답 파싱 단순화

- **목표**: 복잡한 HTTP 상태 코드 분기(404, 429, 500, 503 등)와 과도한 예외 안내문을 제거하고, 통신 실패 시 오류 정보를 담백하게 전달하는 단순한 구조로 변경합니다.
- **요구사항**:
  - 에러 발생 시, 구글 API가 반환한 Response body에서 `영어 에러 메시지(error message)`, `에러 유형/이름(status)`, `상태 코드(code)`를 안전하게 가져오는 변수 세 가지를 활용하여 최소한의 명확한 정보만 보여줍니다.
  - 과다한 `if e.code == ...` 분기문을 지우고, Response의 에러 JSON 객체에서 에러에 대한 핵심적인 세 가지 변수 정보를 추출해 에러 문구로 반환하는 로직으로 간소화하십시오.

