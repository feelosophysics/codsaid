# ==============================================================================
# 파일명: study/study_code/gitgen/__init__.py
# 목적: gitgen 디렉토리를 파이썬 패키지(Package)로 정의하고 초기화하기 위한 파일입니다.
# ==============================================================================

"""
[파이썬 패키지(Package)와 모듈(Module) 기초]

1. 모듈(Module)이란?
   - 파이썬 코드가 저장된 '.py' 파일 하나를 의미합니다.
   - 변수, 함수, 클래스 등을 정의해 두고 다른 코드에서 가져다(import) 쓸 수 있습니다.

2. 패키지(Package)란?
   - 여러 개의 모듈(.py 파일)을 하나의 폴더 구조로 묶어놓은 것입니다.
   - 폴더 형태로 관리하여 네임스페이스(Namespace)를 체계적으로 분리하고 이름 충돌을 방지합니다.

3. __init__.py 파일의 역할
   - 파이썬 3.3 이전 버전에서는 폴더 안에 __init__.py 파일이 반드시 존재해야만 파이썬이 해당 폴더를 '패키지'로 인식했습니다.
   - 파이썬 3.3부터는 이 파일이 없어도 '네임스페이스 패키지(Namespace Package)'로 인식하지만,
     하위 호환성 유지, 패키지 초기화 로직 수행, 외부 노출 인터페이스(API) 정의 등을 위해 여전히 널리 사용됩니다.
   - 본 파일은 gitgen 패키지가 임포트될 때(예: import gitgen) 가장 먼저 자동으로 단 한 번 실행됩니다.

4. 컴퓨터 과학(CS) 관점: 모듈화(Modularity)와 응집도(Cohesion)
   - 모듈화는 복잡한 소프트웨어를 기능별로 작고 독립된 단위로 쪼개어 개발하는 기법입니다.
   - 이 파일이 위치한 gitgen 패키지는 'AI 기반 Git 메시지/PR 자동 생성'이라는 명확한 하나의 목적을 가진 응집도 높은 모듈들의 집합체입니다.
"""

# 패키지의 공식 문서 설명글(Docstring) 정의
# 이 파일 자체와 gitgen 패키지가 수행하는 핵심 역할을 문서화한 것입니다.
"""gitgen 패키지 초기화 파일입니다.

이 패키지는 AI 기반 Git 커밋 및 PR 초안 생성을 위한 모듈들(config, gemini_client, git_helper, prompt_templates, safe_mode)로 구성되어 있습니다.
"""
